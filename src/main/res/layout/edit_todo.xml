<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="70dp"
    android:orientation="horizontal">
    <RelativeLayout
        android:paddingStart="6dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/img_todo_on"
        android:paddingTop="1dp"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:src="@drawable/ic_todo_off"
        android:visibility="visible"/>
        <ImageView
            android:id="@+id/img_todo_off"
            android:paddingTop="1dp"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:src="@drawable/ic_todo_on"
            android:visibility="gone"/>

    </RelativeLayout>

    <com.sprd.notejar.view.CustomEditText
        android:id="@+id/et_todo"
        android:layout_width="match_parent"
        android:layout_height="28dp"
        android:background="@null"
        android:textAlignment="viewStart"
        android:imeOptions="actionNext"
        android:inputType="textMultiLine|textCapSentences"
        android:lineSpacingExtra="8dp"
        android:autoLink="email|web|phone"
        android:paddingStart="6dp"
        android:visibility="visible" />
    <!--android:textCursorDrawable="@drawable/cursor_shape_normal"-->
</LinearLayout>